[project]
name = "cpiano"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.1",
    "opencv-python>=5.0.0.93",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
